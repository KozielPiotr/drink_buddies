"""Configuration for drinking_auth application."""

from django.apps import AppConfig


class DrinkingAuthConfig(AppConfig):
    """Basic app config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "drinking_buddies.drinking_auth"
